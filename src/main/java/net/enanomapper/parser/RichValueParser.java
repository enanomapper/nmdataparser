package net.enanomapper.parser;

import net.enanomapper.parser.RecognitionUtils.RichValue;

public class RichValueParser 
{
	public RichValue parse(String rvStr)
	{
		RichValue rvalue = new RichValue();
		int nChar = rvStr.length();
		int pos = 0;
		while (pos < nChar)
		{
			char ch = rvStr.charAt(pos);
			
			
		}
		
		
		return rvalue;
	}
}
